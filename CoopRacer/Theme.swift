import SwiftUI
import SpriteKit

enum Theme {
    static let p1 = Color(red: 1.0, green: 0.18, blue: 0.22)   // bright red
    static let p2 = Color(red: 0.16, green: 0.44, blue: 1.0)   // bright blue

    static let p1SK = SKColor(red: 1.0, green: 0.18, blue: 0.22, alpha: 1.0)
    static let p2SK = SKColor(red: 0.16, green: 0.44, blue: 1.0, alpha: 1.0)
}
